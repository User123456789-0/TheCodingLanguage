# TheCodingLanguage
this will download a coding language and run it with a .CodingLanguage file (drag the .CodingLanguage file to the lancher, that how you run it), i cannot edit the registry to dobble click the .CodingLanguage and then it will run, that one i cannot do.
